name: aqi_monitor
description: IoT AQI Monitoring App with Firebase Real-time Database
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_database: ^10.3.6
  fl_chart: ^0.66.0
  intl: ^0.18.1
  google_fonts: ^6.1.0
  geolocator: ^11.0.0
  geocoding: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
