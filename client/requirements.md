## Packages
firebase | For real-time database connection (AQI data source)
recharts | For the history line chart visualization
date-fns | For time formatting (e.g., "just now", "5m ago")
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
